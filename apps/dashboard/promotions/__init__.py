default_app_config = (
    'oscar.apps.dashboard.promotions.config.PromotionsDashboardConfig')

# Note (TODO):
# The whole app was copied over for modifying the PagePromotionForm to include link_url
# Once the fix is made for django-oscar and released, we can delete the promotions app.
